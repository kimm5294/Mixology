<div class="container">
  <h3>Search Results</h3>
  <ul>
    <% @results.each do |result| %>
      <li>
        <%=link_to "#{result["name"]}", {controller: "drinks", action: "display", api: "#{result["id"]}"} %>
      </li>
    <% end %>
  </ul>
</div>